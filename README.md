# recommend-model-skill

Copilot cli skill which recommends a model for a specific job, based on price-performance data

## Installation

1. Clone the repo
1. Copy the folder `recommend-model` to `~/.copilot/skills`
